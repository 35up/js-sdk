{
  "name": "@35up/js-sdk--monorepo",
  "private": "true",
  "scripts": {
    "bump-version": "lerna version --no-git-tag-version --no-push",
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "mock-server": "mock-server"
  },
  "devDependencies": {
    "js-sdk-mock-server": "file:mock-server",
    "lerna": "^5.5.2",
    "nx": "^14.7.16"
  },
  "workspaces": [
    "packages/*"
  ]
}
